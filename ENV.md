# ⚙️ Environment Variables — সম্পূর্ণ গাইড

এই ফাইলে বটের **প্রতিটি** সেটিং ব্যাখ্যা করা আছে: কী, কেন দরকার, না দিলে কী হবে, আর উদাহরণ।

> **কোথায় বসাবেন?** তিন জায়গার যেকোনো একটি (উপরেরটা আগে দেখা হয়):
> ১. প্রজেক্ট ফোল্ডারে **`config.env`** ফাইল (সবচেয়ে সহজ — `config.env.example` কপি করে বানান)
> ২. হোস্টিং প্যানেলের **Environment Variables** সেকশন (যেমন cPanel/Plesk/জেলাস্টিক/Railway/VPS systemd)
> ৩. শেল থেকে: `export BOT_TOKEN=...` (অস্থায়ী, রিস্টার্টে হারাবে)
>
> ⚠️ কোনো ক্রেডেনশিয়াল কোড বা README-তে লিখবেন না। `config.env` ফাইলটি `.gitignore`-এ আছে, তাই গিটে যাবে না।

---

## ১. বাধ্যতামূলক (এগুলো ছাড়া বট চালু হবে না)

| ভেরিয়েবল | কী | না দিলে | উদাহরণ |
|---|---|---|---|
| `API_ID` | আপনার Telegram অ্যাপের আইডি (my.telegram.org → API development tools) | বট চালু হয় না, লগে "API_ID is missing" | `API_ID=1234567` |
| `API_HASH` | একই অ্যাপের হ্যাশ (৩২ অক্ষরের হেক্স) | বট চালু হয় না | `API_HASH=0123456789abcdef0123456789abcdef` |
| `BOT_TOKEN` | @BotFather-এর টোকেন | বট কানেক্ট হয় না | `BOT_TOKEN=123456:AA...` |
| `ADMIN_IDS` | প্যানেল ব্যবহারকারী Telegram আইডি (কমা দিয়ে একাধিক) | `/admin` কাউকে খুলবে না, ফাইল সাজানো যাবে না | `ADMIN_IDS=8768764605,7813556801` |

**`ADMIN_IDS` কীভাবে বের করবেন?** নিজের প্রোফাইল লিংক/`@userinfobot` দিয়ে আইডি দেখা যায়। প্রথম আইডিটিই "প্রাইমারি অ্যাডমিন" — সে প্রথমবার চালুতে `STRING_SESSION` পায়।

---

## ২. সেশন (প্রাইভেট চ্যানেলের ফাইল পাঠাতে)

| ভেরিয়েবল | কী | না দিলে | উদাহরণ |
|---|---|---|---|
| `STRING_SESSION` | আগে থেকে বানানো userbot session, প্রথম চালুতে সেট হয় | /session দিয়ে হাতে সেশন দিতে হবে (বটের মধ্যে) | `STRING_SESSION=1BVts...` |

আপনার সব সেশন এখন **ডেটাবেসের `sessions` টেবিলে** জমা থাকে এবং বট রিস্টার্ট হলে নিজে থেকেই রিকানেক্ট (সেশন ওয়াচডগ) হয়।

---

## ৩. ফোর্স-জয়েন ও ক্যাপশন

| ভেরিয়েবল | কী | না দিলে | উদাহরণ |
|---|---|---|---|
| `FORCE_CHANNEL` | ইউজারকে ফাইল পেতে আগে যে চ্যানেলে জয়েন করতে হবে | কেউ জয়েন ছাড়াই সব ফাইল পাবে | `FORCE_CHANNEL=@mychannel` |
| `CUSTOM_CAPTION` | প্রতিটি ডেলিভারি ফাইলের সাথে যাওয়া ক্যাপশন | ফাইল বিনা ক্যাপশনে যাবে | `CUSTOM_CAPTION=আমাদের চ্যানেল জয়েন করুন` |

> 💡 বটকে ওই চ্যানেলে অ্যাড রাখুন, নাহলে চেক করতে গিয়ে FloodWait বা ভুল ফলাফল আসবে। চেকের ফল ১০ মিনিট ক্যাশ হয় (`FORCE_JOIN_CACHE_SECONDS`)।

---

## ৪. পেমেন্ট (কেনা-বেচার জন্য — এখানে দিলেই ইউজার bKash/Nagad নাম্বার দেখতে পাবে)

| ভেরিয়েবল | কী | না দিলে | উদাহরণ |
|---|---|---|---|
| `CURRENCY` | দামের প্রতীক | `৳` ধরে নেবে | `CURRENCY=৳` বা `CURRENCY=Tk` |
| `PAY_BKASH` | বিকাশ নাম্বার (নম্বর/টাইপ) | "Unlock" চাপলে "পেমেন্ট নাম্বার সেট করা নেই" দেখাবে | `PAY_BKASH=01712-345678 (personal)` |
| `PAY_NAGAD` | নগদ নাম্বার | ওই অপশন দেখাবে না | `PAY_NAGAD=01812-000000 (personal)` |
| `PAY_ROCKET` | রকেট নাম্বার | অপশন লুকিয়ে যাবে | `PAY_ROCKET=01712345678-1` |
| `PAY_UPI` | UPI আইডি (ভারত হলে) | অপশন লুকিয়ে যাবে | `PAY_UPI=yourname@upi` |
| `PAY_CRYPTO` | ক্রিপ্টো ওয়ালেট (USDT-TRC20 ইত্যাদি) | অপশন লুকিয়ে যাবে | `PAY_CRYPTO=TXYZ...address` |
| `PAY_NOTE` | পেমেন্ট স্ক্রিনে অতিরিক্ত নির্দেশ (বাংলায় লিখতে পারেন) | ঘরটি ফাঁকা থাকবে | `PAY_NOTE=টাকা পাঠিয়ে স্ক্রিনশট দিন` |

**কীভাবে কাজ করে:** ইউজার 🔓 Unlock চাপে → প্ল্যান দেখে → নাম্বার দেখে টাকা পাঠায় → **স্ক্রিনশট/ট্রানজেকশন আইডি** বটকে পাঠায় → আপনার কাছে "🧾 New payment" অ্যালার্ট আসে ✅/❌ বাটন সহ → ✅ দিলেই ইউজারের অ্যাক্সেস চালু + তাকে নোটিফিকেশন। অ্যাডমিন প্যানেলের 🧾 Orders বা ওয়েব ড্যাশবোর্ড থেকেও অ্যাপ্রুভ করা যায়।

> `PAY_*` ফাঁকা থাকলে ইউজারের প্ল্যান চাওয়া বন্ধ থাকে না — সে "Unlock" চেপে অ্যাডমিনের সাথে কন্টাক্ট করতে পারবে।

---

## ৫. কন্টাক্ট / সাপোর্ট

| ভেরিয়েবল | কী | না দিলে | উদাহরণ |
|---|---|---|---|
| `SUPPORT_CONTACT` | কন্টাক্ট স্ক্রিনে দেখানো আপনার ইউজারনেম/আইডি | স্ক্রিনে শুধু "লিখে পাঠান" দেখাবে (বটের মেসেজ ফিচার কাজ করবে) | `SUPPORT_CONTACT=@myusername` |
| `SUPPORT_NOTE` | যোগাযোগের সময়/শর্ত | কিছুই দেখাবে না | `SUPPORT_NOTE=সকাল ১০টা - রাত ১০টা` |
| `TICKET_COOLDOWN` | একই ইউজার কত সেকেন্ড পর নতুন মেসেজ পাঠাতে পারবে | ৬০ সেকেন্ড | `TICKET_COOLDOWN=120` |

**কীভাবে কাজ করে:** ইউজার 📞 অ্যাডমিন কন্টাক্ট চাপে → মেসেজ লেখে → আপনার কাছে টিকেট আকারে আসে (✍️ Reply বাটন) → আপনি উত্তর দিলে ইউজার সাথে সাথে পায়। প্যানেলের 📩 Support-এ সব খোলা টিকেট জমা থাকে।

---

## ৬. ট্রায়াল ও অটো-অ্যাপ্রুভাল

| ভেরিয়েবল | কী | না দিলে | উদাহরণ |
|---|---|---|---|
| `TRIAL_HOURS` | প্রিমিয়াম স্টোরে ফ্রি ট্রায়ালের দৈর্ঘ্য (প্রতি ইউজার প্রতি স্টোরে একবার) | ২৪ ঘণ্টা | `TRIAL_HOURS=6` (কম দিলে বেশি সেল) / `0` দিলে ট্রায়াল বন্ধ |
| `AUTO_APPROVE_ZERO` | দাম ০ টাকার প্ল্যান নিজে থেকেই চালু হবে কি | চালু (true) | `AUTO_APPROVE_ZERO=false` |

---

## ৭. আচরণ ও পারফরম্যান্স

| ভেরিয়েবল | ডিফল্ট | কাজ | কখন বদলাবেন |
|---|---|---|---|
| `TZ_OFFSET` | `6.0` | আপনার টাইমজোন। ড্রিপ, রিমাইন্ডার, লগ সব এই সময়ে। সার্ভার বিদেশে থাকলেও সমস্যা নেই | বাংলাদেশ = ৬.০ |
| `CONTENT_PAGE_SIZE` | `12` | এক পাতায় কতটি ফাইল বাটন | ফাইল নাম লম্বা হলে ৮-১০ |
| `SEARCH_RESULT_LIMIT` | `12` | সার্চে সর্বোচ্চ ফলাফল | বেশি দিলে মেসেজ বড় হবে |
| `FORCE_JOIN_CACHE_SECONDS` | `600` | ফোর্স-জয়েন চেক কত সেকেন্ড ক্যাশ থাকবে | FloodWait এলে বাড়ান (১৮০০) |
| `JIT_TIMEOUT_SECONDS` | `10` | প্রাইভেট চ্যানেল থেকে ফাইল আনার অপেক্ষা | ধীর চ্যানেলে ১৫-২০ |
| `CLEANUP_JIT_COPY` | `true` | বটের DM-এ জমা টেম্প কপি পাঠানোর পর মুছে ফেলা | কোনো কারণে ফাইল না গেলে `false` করে দেখুন |
| `BROADCAST_DELAY` | `0.35` | প্রতি মেসেজে কত সেকেন্ড বিরতি | FloodWait এলে ০.৫-১.০ |
| `DRIP_SEND_DELAY` | `0.5` | ড্রিপে প্রতি ফাইলে বিরতি | একই |
| `FLOOD_SAFETY_MAX` | `900` | এর চেয়ে বড় FloodWait এলে রিট্রাই না করে থেমে যাবে | ডিফল্ট রাখুন |

---

## ৮. ইনলাইন মোড (ভাইরাল হওয়ার সবচেয়ে বড় হাতিয়ার)

| ভেরিয়েবল | ডিফল্ট | কাজ |
|---|---|---|
| `INLINE_ENABLED` | `true` | যেকোনো চ্যাটে `@YourBot interstellar` লিখে ফাইল খোঁজা |
| `INLINE_RESULTS` | `20` | সর্বোচ্চ ফলাফল |

**চালু করতে ৩ ধাপ:** @BotFather → `/setinline` → বট বেছে নিয়ে placeholder দিন (যেমন `🔎 ফাইলের নাম লিখুন`) → বট রিস্টার্ট। **প্রিমিয়াম ফাইল ইনলাইনে দেখা যায় না** — অ্যাক্সেস না থাকলে রেজাল্ট লুকানো থাকে।

---

## ৯. ভাষা

| ভেরিয়েবল | ডিফল্ট | কাজ |
|---|---|---|
| `DEFAULT_LANG` | `bn` | নতুন ইউজারের ভাষা (`bn` বাংলা / `en` English) |

ইউজার নিজেই 🌐 বাটনে চাপ দিয়ে যেকোনো সময় বদলাতে পারবে।

---

## ১০. ওয়েব ড্যাশবোর্ড (ঐচ্ছিক)

| ভেরিয়েবল | ডিফল্ট | কাজ |
|---|---|---|
| `WEB_ENABLED` | `false` | ড্যাশবোর্ড চালু/বন্ধ |
| `WEB_HOST` | `0.0.0.0` | কোন ঠিকানায় শুনবে (হোস্টিং-এ `0.0.0.0` দরকার — নাহলে বাইরে থেকে দেখা যাবে না) |
| `WEB_PORT` | `8080` | পোর্ট |
| `WEB_USER` | `admin` | লগইনের ইউজারনেম |
| `WEB_PASS` | *(খালি)* | লগইনের পাসওয়ার্ড — **খালি থাকলে ড্যাশবোর্ড চালুই হবে না** (সুরক্ষা) |
| `WEB_SECRET` | *(খালি)* | ভবিষ্যতের সেশন/CSRF কী |

চালাতে: `pip install -r requirements-web.txt` তারপর `python -m web.dashboard`

---

## ১১. ডেটা ও ব্যাকআপ

| ভেরিয়েবল | ডিফল্ট | কাজ |
|---|---|---|
| `DB_FILE` | `bot_data.sqlite3` | মূল ডেটাবেস |
| `LEGACY_DB_FILE` | `bot_db.json` | পুরোনো JSON — প্রথম চালুতে একবারই অটো-ইমপোর্ট হয় |
| `BACKUP_DIR` | `backups` | দৈনিক ব্যাকআপ ফোল্ডার (JSON + SQLite কপি) |
| `LOG_FILE` | `logs/bot.log` | রোটেটিং লগ |
| `SESSION_STATE_FILE` | `bot_master.session` | বটের নিজের সেশন ফাইল |
| `BACKUP_HOUR` | `4` | কয়টার সময় (আপনার TZ) দৈনিক ব্যাকআপ হবে |
| `KEEP_BACKUPS` | `7` | সর্বোচ্চ কত দিনের ব্যাকআপ রাখবেন |
| `GROUPS_PAGE_SIZE` / `USERS_PAGE_SIZE` / `MANAGE_PAGE_SIZE` | `8` / `10` / `10` | বিভিন্ন তালিকার পাতা প্রতি আইটেম |

> ড্যাশবোর্ডের `/health` এন্ডপয়েন্ট (লগইন ছাড়াই) আপটাইম মনিটরের সাথে ব্যবহার করতে পারেন।

---

## ১২. দ্রুত সেটআপ চেকলিস্ট

```
[ ] API_ID, API_HASH, BOT_TOKEN  →  এগুলো ছাড়া কিছুই চলবে না
[ ] ADMIN_IDS                    →  আপনার নিজের আইডি অবশ্যই রাখুন
[ ] STRING_SESSION (বা পরে /session)  →  প্রাইভেট চ্যানেলের ফাইল পাঠাতে
[ ] PAY_BKASH / PAY_NAGAD        →  টাকা নিতে
[ ] SUPPORT_CONTACT              →  ইউজার যেন যোগাযোগ করতে পারে
[ ] FORCE_CHANNEL                →  চ্যানেল গ্রো করতে
[ ] TRIAL_HOURS / CURRENCY       →  সেলের কৌশল
[ ] INLINE_ENABLED + BotFather /setinline  →  ভাইরাল হওয়ার জন্য
[ ] WEB_PASS (যদি ড্যাশবোর্ড চান)
```
