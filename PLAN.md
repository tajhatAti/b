# 🤖 বট v2 — অডিট, নতুন রূপ ও ফিচার পরিকল্পনা

> ফাইল: `tmpb2hxvux6_haven.py` (১৫৫১ লাইন, Telethon-based Telegram File Store Bot)
> তারিখ: ২০২৬-০৯-১৯

---

## 0. 🚨 সবার আগে: জরুরি সিকিউরিটি সমস্যা

আপনার GitHub রিপো **`tajhatAti/b` পাবলিক** (কেউ দেখতে পারবে), আর ওই ফাইলের **১৬–২৬ নম্বর লাইনে** সত্যিকারের লাইভ ক্রেডেনশিয়াল সোজা লেখা আছে:

| লাইন | কী | ঝুঁকি |
|---|---|---|
| ১৬–১৭ | `API_ID` / `API_HASH` | মাঝারি |
| ১৮ | `BOT_TOKEN = "<redacted>"` | **বটের পুরো কন্ট্রোল হাইজ্যাক** |
| ২৬ | `STRING_SESSION = "<redacted>"` | **☠️ আপনার পুরো Telegram অ্যাকাউন্ট দখল হতে পারে** — কারও দরকার নেই শুধু এই স্ট্রিং কপি করা |

একটা string session মানে কেউ আপনার অ্যাকাউন্টে লগইন করতে পারবে — OTP লাগবে না। এখনই যা করবেন:

1. **Telegram → Settings → Devices → সব অচেনা সেশন Terminate করুন** (এতে পুরোনো string session মরে যাবে)।
2. **@BotFather → `/mybots` → আপনার বট → API Token → Revoke** করে নতুন টোকেন নিন।
3. যেহেতু `API_HASH`-ও ফাঁস হয়েছে, চাইলে [my.telegram.org](https://my.telegram.org) → API development tools থেকে নতুন app বানিয়ে নতুন `api_id`/`api_hash` নিন।
4. `bot_db.json` কখনো Git-এ কমিট করবেন না — ওতে সব অ্যাডমিনের সেশন প্লেইন টেক্সটে জমা হয়।

কোডে আমি ইতিমধ্যে `.gitignore` দিয়ে ফাইলগুলো ব্লক করে দিয়েছি, কিন্তু **পুরোনো কমিট থেকে সিক্রেট মুছে যাবে না — তাই রোটেশন ছাড়া কোনো উপায় নেই।**

---

## 1. এখন বটে যা যা আছে (বর্তমান অবস্থা)

- ✅ স্টোর/ক্যাটাগরি তৈরি, স্ক্যান করে ফাইল যোগ (ইউজারবট সেশন + বট-অ্যাডমিন — দুই মোড)
- ✅ JIT ফরওয়ার্ডিং কৌশল (প্রাইভেট চ্যানেল থেকে বট ফাইল পাঠাতে পারে)
- ✅ ডিপ-লিংক: ফাইল আইডি, স্টোর (`STORE_slug`), মাল্টি-ফাইল (`LINK_token`), ব্যাচ (`BATCH_`)
- ✅ প্রিমিয়াম অ্যাক্সেস (শুরু/মেয়াদসহ), রেফারেল রিওয়ার্ড, ডেইলি ড্রিপ, ব্রডকাস্ট ফিল্টার, ফোর্স-জয়েন, কাস্টম ক্যাপশন
- ✅ অ্যাডমিন প্যানেল (ইনলাইন বাটন), ইউজার প্যানেল, সার্চ, ভিউ কাউন্ট, অল-ইউজার লিস্ট

ভিত্তি ভালো — কিন্তু **নতুন ফিচারের আগে কয়েকটা বাগ ঠিক করা জরুরি**, নইলে ফিচার বাড়ালে ঝামেলাও বাড়বে।

---

## 2. 🐞 এখনই যে বাগগুলো কাজ নষ্ট করছে

| # | 심각도 | সমস্যা | কারণ (কোডে) | সমাধান |
|---|---|---|---|---|
| ১ | 🔴 | একবার রিস্টার্ট হলে **ব্রডকাস্ট "All Users"-এ কেউ থাকে না** | `all_bot_users` কখনো DB-তে সেভ হয় না (`save_db()`-এ নেই) | `user_details` থেকেই লিস্ট নিন; বা সেভ করুন |
| ২ | 🔴 | **৪+ ফাইলের ব্যাচ লিংক কাজ করে না** | Telegram-এর `?start=` পেলোড সর্বোচ্চ **৬৪ বাইট**, `BATCH_FILE_.._AND_FILE_..` সীমা ছাড়ায় (line 1436) | শর্ট টোকেন: `BATCH_<10hex>` → টোকেন DB-তে |
| ৩ | 🔴 | **প্রিমিয়াম স্টোরের ফাইল ড্রিপে ফ্রি চলে যায়** | `drip_scheduler` → `send_stored_media_to_uid()`-এ `has_store_access()` চেক নেই (line 1440+) | সাবস্ক্রাইবার লিস্টে ঢোকার সময় + পাঠানোর সময় দুবার অ্যাক্সেস যাচাই |
| ৪ | 🔴 | **লম্বা/বাংলা নামের স্টোরে সব বাটন ভেঙে যায়** | `ustore:{admin}:{name}`, `spg:...` কলব্যাক ডেটা ৬৪ বাইট ছাড়ায় → `BUTTON_DATA_INVALID` | স্টোরকে শর্ট `store_id` দিন; কলব্যাকে নাম নয়, আইডি পাঠান |
| ৫ | 🟠 | ফাইল পাঠানোর **প্রতি ক্লিকে পুরো DB ডিস্কে লেখা হয়** | `bump_views()` → `save_db()` (line 330) | ভিউ মেমোরিতে রাখুন, ৩০ সেকেন্ডে একবার ফ্লাশ |
| ৬ | 🟠 | **JIT ফরওয়ার্ড করা ফাইল বটের DM-এ জমতে থাকে** | `r_msg` পাঠানোর পর ডিলিট করা হয় না (line 375) | সেন্ডের পরপর `await r_msg.delete()` |
| ৭ | 🟠 | **ফ্রি ইউজার প্রিমিয়াম ড্রিপে সাবস্ক্রাইব করে কনটেন্ট পায়** | `notify_toggle:`-এ অ্যাক্সেস চেক নেই (line 1289) | অ্যাক্সেস থাকলেই সাবস্ক্রাইব বাটন দেখান |
| ৮ | 🟠 | স্টোর ডিলিট করলে **অর্ফান ডেটা** থেকে যায় | `del_act`-এ slug, premium, drip, temp_links পরিষ্কার হয় না (line ~685) | সেন্ট্রাল `delete_store()` সার্ভিস |
| ৯ | 🟠 | রিস্টার্ট/ব্রডকাস্টে **DB করাপ্ট হওয়ার ঝুঁকি** | সরাসরি `open(DB_FILE,'w')` — লেখার মাঝপথে ক্র্যাশ হলে ফাইল ভাঙে | temp file + `os.replace()` (atomic), ব্যাকআপ রোটেশন |
| ১০ | 🟠 | **প্রতি ক্লিকে ফোর্স-জয়েন চেক** → FloodWait | `GetParticipantRequest` প্রতি ক্লিকে (line 403) | ৫–১০ মিনিট ক্যাশ |
| ১১ | 🟡 | ব্রডকাস্ট **হ্যাং করে বসে থাকে**, প্রগ্রেস নেই, retry নেই | সিরিয়াল লুপ, FloodWait এড়ানো নেই (line ~1145) | কিউ + FloodWait রিট্রাই + লাইভ প্রগ্রেস বার + রিজিউম |
| ১২ | 🟡 | `1/3` পেজ বাটনে ক্লিক করলে **স্পিনার ঘুরতেই থাকে** | `noop` কলব্যাকের `answer()` করা হয় না | গ্লোবাল হ্যান্ডলার |
| ১৩ | 🟡 | **রেফারেলে সব অ্যাডমিনের রিওয়ার্ড একসাথে** দেওয়া হয় | `grant_referral_reward` সব `referral_config` লুপ করে (line 314) | যার লিংকে এসেছে, শুধু তারই স্টোর |
| ১৪ | 🟡 | ইনপুটের মাঝপথে **ক্যান্সেল করার উপায় নেই** | `admin_input_state` ক্লিয়ার হয় শুধু পরের মেসেজে | `/cancel` কমান্ড |
| ১৫ | 🟡 | মেয়াদ-শেষ হওয়া টেম্প লিংক জমতে থাকে | কেউ ক্লিক না করলে ক্লিনআপ নেই | প্রতি ঘণ্টায় GC টাস্ক |
| ১৬ | 🟡 | স্ক্যান অ্যাডমিন প্যানেল **ব্লক করে** (হাজার ডায়ালগ = বাটন জমে যায়) | `iter_dialogs()` কলব্যাকের ভিতরে `await` (line ~880) | ব্যাকগ্রাউন্ড + "লোড হচ্ছে" প্রগ্রেস |

---

## 3. 🏗️ "নতুন রূপ" — আর্কিটেকচার পরিবর্তনের প্রস্তাব

এখন সব একটা ফাইলে, সব গ্লোবাল ডিকশনারিতে, সেভ হচ্ছে একটা JSON-এ। ১৫০০+ লাইনে এটা ম্যানেজ করা কঠিন। প্রস্তাব:

```
telegram-store-bot/
├── .env.example            # সব সিক্রেট env-এ (কোডে আর কিছুই হার্ডকোড নয়)
├── requirements.txt
├── Dockerfile / compose.yml
├── bot.py                  # এন্ট্রি পয়েন্ট
└── app/
    ├── config.py           # env লোড, ভ্যালিডেশন
    ├── db.py               # SQLite + WAL, মাইগ্রেশন
    ├── keyboards.py        # সব ইনলাইন কীবোর্ড বিল্ডার (একই ডিজাইন-সিস্টেম)
    ├── texts.py            # সব টেক্সট/ভাষা (i18n-ready)
    ├── models.py           # Store, File, User, Plan, Grant, Drip, Broadcast
    ├── services/
    │   ├── sender.py       # ⭐ একটাই ফাইল-সেন্ডিং সার্ভিস (JIT + বট ডাইরেক্ট + ডিলিট + রিট্রাই)
    │   ├── ratelimit.py    # গ্লোবাল কিউ: FloodWait হলে পুরো বট অপেক্ষা করে, ব্রেক করে না
    │   ├── scanning.py     # চ্যানেল স্ক্যানার (প্রগ্রেস + রিজিউম + ডুপ্লিকেট স্কিপ)
    │   ├── scheduler.py    # ড্রিপ, মেয়াদ শেষের রিমাইন্ডার, ব্রডকাস্ট কিউ, GC
    │   └── stats.py        # ভিউ, ইউজার, আয়, টপ ফাইল
    └── handlers/
        ├── user.py         # /start, ব্রাউজ, সার্চ, ফেভারিট, রেফারেল
        ├── admin.py        # প্যানেল ও সব অ্যাডমিন ফ্লো
        ├── content.py      # ফাইল ম্যানেজার (rename/delete/move)
        ├── payment.py      # প্ল্যান কেনা, রিসিট অ্যাপ্রুভাল
        └── callbacks.py    # কলব্যাক রাউটার (এক জায়গায় map, যাতে বাটন-ডেটা আর না ভাঙে)
```

**ডেটাবেস: JSON → SQLite (WAL)।** কারণ: ইউজার বাড়লে এখনকার `save_db()` (পুরো ফাইল রিরাইট) অসহনীয় হবে; ব্রডকাস্ট/স্ট্যাটস/সার্চে ইনডেক্স দরকার; আর অ্যাডমিন প্যানেল থেকে অ্যাসিঙ্ক কোয়েরি করা যাবে। **পুরোনো `bot_db.json` থেকে অটো-মাইগ্রেশন স্ক্রিপ্ট** দেব — আপনার বর্তমান ডেটা হারাবে না।

চাইলে আরেক ধাপ উপরে: **ছোট ওয়েব ড্যাশবোর্ড** (FastAPI + একটা পেজ) — ব্রাউজার থেকে ফাইল আপলোড/রিনেম/ডিলিট, ইউজার ও আয় দেখা, ব্রডকাস্ট চালানো। এটা দিলে অনেক কাজ Telegram প্যানেলের বদলে মাউসে হয়ে যাবে।

---

## 4. ✨ নতুন ফিচার — গ্রুপ অনুযায়ী

### A. অ্যাডমিনের কাজ সহজ করতে (আপনার দিন বাঁচাবে)
1. **ফাইল ম্যানেজার**: রিনেম, ডিলিট, এক স্টোর থেকে আরেক স্টোরে মুভ, বাল্ক সিলেক্ট → একসাথে ডিলিট/মুভ।
2. **স্টোর সেটিংস**: কভার ছবি, বর্ণনা, প্রাইসিং, সর্টিং নিয়ম (নতুন/জনপ্রিয়), আলাদা ক্যাপশন, প্রাইভেট/পাবলিক।
3. **ডুপ্লিকেট ডিটেক্ট**: একই `chat_id + msg_id` দুইবার যোগ হলে স্কিপ।
4. **স্ক্যান রিজিউম + অটো-সিঙ্ক**: নতুন পোস্ট এলে চ্যানেল থেকে অটো যোগ (কোনো ক্লিক ছাড়াই)।
5. **ব্রডকাস্ট v2**: প্রগ্রেস বার, FloodWait-এ অটো রিট্রাই+রিজিউম, শিডিউল করা ব্রডকাস্ট, ছবি/ভিডিও সহ ব্রডকাস্ট, PIN option।
6. **আপনার নিজের স্ট্যাটস ড্যাশবোর্ড**: ইউজার গ্রোথ, কে সবচেয়ে বেশি ডাউনলোড করছে, টপ/ডেড ফাইল, আয়, কনভার্শন।
7. **CSV/JSON এক্সপোর্ট-ইমপোর্ট** — ব্যাকআপ/মাইগ্রেশন সহজ।
8. **মাল্টি-সেশন পুল**: একাধিক সেশন যোগ করলে অটো রোটেট হবে; একটা FloodWait-এ পড়লে অন্যটা কাজ করবে।
9. **হেলথ মনিটর**: সেশন ডিসকানেক্ট হলে সাথে সাথে অ্যাডমিনকে অ্যালার্ট + অটো রিকানেক্ট।
10. **অটো-ব্যাকআপ**: প্রতিদিন DB স্ন্যাপশট, `/restore` দিয়ে ফিরিয়ে আনা।
11. **দুই-ধাপে কনফার্মেশন** সব ডিস্টাক্টিভ অ্যাকশনে (ডিলিট, ব্রডকাস্ট) — ভুল ক্লিকে দুর্ঘটনা বন্ধ।

### B. ইউজারের অভিজ্ঞতা (রিটেনশন বাড়ে)
12. **ইনলাইন মোড**: যেকোনো চ্যাটে `@YourBot filmy name` লিখলেই রেজাল্ট ক্যালোগ, ট্যাপে ফাইল — ভাইরাল হওয়ার সবচেয়ে বড় হাতিয়ার।
13. **নতুন ব্রাউজিং ডিজাইন**: মেইন মেনু → ক্যাটাগরি → সাব-ক্যাটাগরি, সাথে ⭐ ফিচার্ড/🆕 নতুন/🔥 ট্রেন্ডিং ট্যাব।
14. **সর্টিং ও ফিল্টার**: নতুন আগে, জনপ্রিয় আগে, টাইপ অনুযায়ী (ভিডিও/ছবি), ভিউ কাউন্ট দেখানো।
15. **ফেভারিট/ওয়াচলিস্ট** — ইউজার সেভ করে রাখতে পারবে।
16. **"পাওয়া যাচ্ছে না?" রিকোয়েস্ট ফিচার** — ইউজার লিখে জানাবে, অ্যাডমিন প্যানেলে লিস্ট হবে (অনেক চ্যানেলের চাহিদা জানার সেরা উপায়)।
17. **শেয়ার বাটন** — আপনার স্টোর লিংক সহ ফরওয়ার্ডেবল কার্ড, যাতে ইউজার নিজের গ্রুপে শেয়ার করে → ফ্রি মার্কেটিং।
18. **EU ভাষা সাপোর্ট**: বাংলা/ইংরেজি/হিন্দি টগল (`texts.py` থেকে)।
19. **নোটিফিকেশন প্রেফারেন্স**: কোন স্টোরের আপডেট চায়, সাপ্তাহিক ডাইজেস্ট।
20. **মেয়াদ শেষের রিমাইন্ডার** (৩ দিন আগে) + রিনিউ বাটন — রিনিউয়াল রেট বাড়ে।

### C. আয় / মনিটাইজেশন (সবচেয়ে বেশি প্রভাব ফেলবে)
21. **সাবস্ক্রিপশন প্ল্যান সিস্টেম**: প্রতি স্টোরে Weekly / Monthly / Yearly / Lifetime, স্পষ্ট প্রাইস, বেনিফিট লিস্ট।
22. **পেমেন্ট গ্রহণ** (ধাপে ধাপে):
    - **ম্যানুয়াল**: bKash/Nagad/UPI নাম্বার দেখিয়ে স্ক্রিনশট নিন → অ্যাডমিন প্যানেলে Approve/Reject → অটো অ্যাক্সেস।
    - **Telegram Stars**: ইন-অ্যাপ পেমেন্ট, কিছুটা ক্লিকেই প্ল্যান কেনা যায়।
    - **ক্রিপ্টো (CryptoBot) / Stripe** চাইলে পরে।
23. **কুপন / ডিসকাউন্ট কোড**, ফ্রি ট্রায়াল (২৪ ঘণ্টা), রেফারেল-এ ছাড়।
24. **Revenue স্ট্যাটস**: কতজন কিনলো, কত আয়, কোন প্ল্যান বেশি চলছে, চর্ন রেট।
25. **অটো-এক্সপায়ার + উইন-ব্যাক**: মেয়াদ শেষে বন্ধুত্বপূর্ণ অফার সহ মেসেজ।
26. **ট্রায়াল ফানেল**: ১–২টা ফ্রি ফাইল → এরপর প্রিমিয়াম (কনভার্শনের ক্লাসিক ট্রিক)।

### D. ভরসা, স্পিড ও স্কেল
27. **সেন্ট্রাল রেট-লিমিটার**: FloodWait হলে বট ক্র্যাশ না করে অপেক্ষা করে, তারপর আবার চালায়।
28. **স্ট্রাকচার্ড লগিং** (ফাইল + কনসোল) + অ্যাডমিনকে এরর অ্যালার্ট (`/logs`)।
29. **SQLite + ইনডেক্স + ব্যাকআপ**: ১০ হাজার ফাইল/লাখ ভিউ হলেও চলবে।
30. **ডিপ্লয়মেন্ট**: Docker + systemd/pm2 সেটআপ, `/ping`, গ্রেসফুল শাটডাউন, অটো-রিস্টার্ট।
31. **টেস্ট**: পেমেন্ট/অ্যাক্সেস/লিংক-জেনারেশনের ইউনিট টেস্ট — নতুন কিছু ভাঙলে সাথে ধরা পড়বে।

---

## 5. 🚀 ধাপে ধাপে রোডম্যাপ

| ফেজ | কী হবে | সময় (আনুমানিক) | লাভ |
|---|---|---|---|
| **০** | সিক্রেট রোটেশন + `.gitignore` + .env-এ কনফিগ | ৩০ মিনিট | অ্যাকাউন্ট হারানোর ঝুঁকি শেষ |
| **১** | 🔴 বাগ #১–৯ ফিক্স + atomic save + noop + ক্যান্সেল + GC | ১ দিন | বট আর ভাঙবে না, লিক বন্ধ |
| **২** | মডুলার স্ট্রাকচার + SQLite + মাইগ্রেশন + লগিং + রেট-লিমিটার | ২–৩ দিন | ভবিষ্যৎ ফিচার দ্রুত যোগ হবে |
| **৩** | ফাইল ম্যানেজার, স্টোর সেটিংস, ব্রডকাস্ট v2, স্ট্যাটস, ব্যাকআপ | ৩–৪ দিন | আপনার দৈনন্দিন কাজ ৭০% কমবে |
| **৪** | ইনলাইন মোড, নতুন UX, ফেভারিট, রিকোয়েস্ট, শেয়ার কার্ড, ভাষা | ৩–৪ দিন | ইউজার ও রিটেনশন বাড়বে |
| **৫** | প্ল্যান + ম্যানুয়াল পেমেন্ট অ্যাপ্রুভাল → Telegram Stars | ৪–৫ দিন | আয় শুরু |
| **৬** | (অপশনাল) ওয়েব ড্যাশবোর্ড, অটো-সিঙ্ক, সেশন পুল | চাহিদা অনুযায়ী | পুরো কন্ট্রোল ব্রাউজারে |

> ফেজ ০–১ একদম প্রথমে করা উচিত — খুব কম পরিশ্রমে সবচেয়ে বড় ঝুঁকি সরবে।

---

## 6. ❓ এখন আপনার সিদ্ধান্ত দরকার

1. স্ট্রাকচার কেমন চান — এক ফাইলেই সরল রাখব, নাকি মডুলার প্রজেক্ট বানাব?
2. কোন ফেজ আগে শুরু করব?
3. JSON রাখব নাকি SQLite-এ নিয়ে যাব?
4. ওয়েব ড্যাশবোর্ড চান কি না।

---

## 7. ✅ বাস্তবায়নের অবস্থা (আপডেট: ২০২৬-০৯-১৯)

ফেজ **০ (সিকিউরিটি)** ও ফেজ **১ (সব বাগ ফিক্স)** শেষ — নতুন মডুলার প্রজেক্ট আকারে।
বিস্তারিত: `README.md`

| # | বাগ | অবস্থা | কোথায় ঠিক হলো |
|---|---|---|---|
| ১ | রিস্টার্টে ইউজার লিস্ট হারানো | ✅ | `storage.py` — `users` টেবিলই এখন সোর্স অব ট্রুথ |
| ২ | ৪+ ফাইল ব্যাচ লিংক ভাঙা | ✅ | `payloads.py` + `links` টেবিল (শর্ট টোকেন) |
| ৩ | প্রিমিয়াম স্টোরের ফাইল ড্রিপে ফ্রি যাওয়া | ✅ | `scheduler.drip_loop` + `access.has_access` চেক |
| ৪ | লম্বা/বাংলা নামে বাটন ভাঙা | ✅ | সব কলব্যাক এখন শর্ট আইডি (`s:12`, `f:345`) + টেস্ট |
| ৫ | প্রতি ক্লিকে পুরো DB লেখা | ✅ | SQLite, `UPDATE files SET views = views + 1` |
| ৬ | JIT কপি বট DM-এ জমা | ✅ | `sender._try_jit` — পাঠানোর পর ডিলিট |
| ৭ | প্রিমিয়াম ড্রিপে ফ্রি সাবস্ক্রিপশন | ✅ | `user.toggle_subscription`-এ অ্যাক্সেস চেক |
| ৮ | স্টোর ডিলিটে অর্ফান ডেটা | ✅ | `storage.delete_store` — ক্যাসকেড পরিষ্কার |
| ৯ | DB করাপ্ট হওয়ার ঝুঁকি | ✅ | SQLite WAL + atomic write + দৈনিক ব্যাকআপ |
| ১০ | প্রতি ক্লিকে ফোর্স-জয়েন চেক (FloodWait) | ✅ | ১০ মিনিট ক্যাশ (`runtime.cached_join`) |
| ১১ | ব্রডকাস্ট হ্যাং, প্রগ্রেস নেই | ✅ | `broadcast.py` — কিউ, রিট্রাই, লাইভ প্রগ্রেস |
| ১২ | `noop` বাটনে স্পিনার ঘোরা | ✅ | `router.dispatch` — `answer()` করে দেয় |
| ১৩ | রেফারেলে সব অ্যাডমিনের রিওয়ার্ড | ⚠️ পুনর্মূল্যায়ন | একাধিক অ্যাডমিনের আলাদা রেফারেল প্রোগ্রাম থাকলে এটা আসলে ঠিক আচরণ — আগের মতোই রাখা হয়েছে, তবে এখন ডকুমেন্টেড |
| ১৪ | ইনপুট ফ্লো ক্যান্সেল করা যায় না | ✅ | `/cancel` + ভুল ইনপুটে আবার জিজ্ঞেস |
| ১৫ | মেয়াদোত্তীর্ণ লিংক জমা | ✅ | `gc_loop` প্রতি ঘন্টায় পরিষ্কার |
| ১৬ | স্ক্যানে প্যানেল ব্লক | ✅ | `scanner.py` ব্যাকগ্রাউন্ড + প্রগ্রেস + Stop |

### বোনাস ফিক্স (অডিটের সময় পাওয়া নতুন সমস্যা)

| সমস্যা | প্রভাব | সমাধান |
|---|---|---|
| **Telethon `send_file(noforwards=True)` চুপচাপ উপেক্ষা করে** | ফাইল ফরওয়ার্ড করা যেত | সরাসরি `SendMediaRequest(noforwards=True)` |
| `parse_mode` সেট ছিল না, অথচ টেক্সটে `**bold**` | ইউজার অ্যাস্টেরিস্ক দেখত | ক্লায়েন্টে `parse_mode="html"` + সব টেক্সট HTML |
| সব স্টোর গ্রান্ট ছাড়া খোলা যেত না | ফ্রি স্টোর মানে সবাই আটকা | প্রতি স্টোরে `is_premium` ফ্ল্যাগ + টগল |
| স্ক্যানে ডুপ্লিকেট যোগ হতো | ভিউ/কাউন্ট ভুল | `chat_id + msg_id` ইউনিক চেক |
| `all_bot_users` মেমোরিতে | (বাগ #১) | ডেটাবেস |
| ব্লক করা ইউজার বারবার এরর | ব্রডকাস্ট ধীর | `blocked_users` স্কিপ লিস্ট |

### পরের ধাপ (ফেজ ২+ — আপনার নির্দেশনা পেলে শুরু করব)

- ফেজ ২: আরও স্ট্যাটস/VIP সেগমেন্টেশন, স্টোর কভার/ডেসক্রিপশন, সর্টিং ও ফিল্টার
- ফেজ ৩: প্ল্যান + পেমেন্ট (bKash/Nagad স্ক্রিনশট অ্যাপ্রুভাল → Telegram Stars), কুপন, ট্রায়াল
- ফেজ ৪: ইনলাইন মোড (`@bot keyword`), ফেভারিট/ওয়াচলিস্ট, রিকোয়েস্ট বক্স, বাংলা/ইংরেজি UI
- ফেজ ৫: ওয়েব ড্যাশবোর্ড, অটো-সিঙ্ক, মাল্টি-সেশন পুল
